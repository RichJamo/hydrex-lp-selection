__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# Generated artifact from retention_scorecard.py --image (regenerate, don't commit)
retention_scorecard.png

# Pages build output (assembled in CI, deployed as an artifact — never committed)
_site/

# Live current-epoch dashboard — regenerated every deploy from the APIs, never committed
live.html
